<!-- temporary build landing page here -->
		<!-- Nav bar -  to be moved to partials -->
	<nav id='nav' class='navbar navbar-default navbar-fixed-top'>
  <div class='container-fluid'>
    <div class='navbar-header'>
      <button type='button' class='navbar-toggle collapsed' data-toggle='collapse' data-target='#bs-navbar'>
        <span class='sr-only'>Toggle navigation</span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
      </button>
      <a class="siteLogo siteLogoNav navbar-brand page-scroll a" href="/">Knock Knock</a>
  
    </div>
    <div class='navbar-collapse collapse' id='bs-navbar'>
      <ul class='nav navbar-nav pull-right'>
        <% if current_user %>
          <% if params[:controller] == 'user' && params[:action] == 'show' %>
          <li>
            <%= link_to 'New Meeting', new_meeting_post_path(@city, @post) %>
          </li>
          <% end %>
          <li>
            <%= link_to 'Profile', user_path(current_user) %>
          </li>
          <li>
            <%= link_to 'Logout', logout_path %>
          </li>
          <% else %>
          <li>
            <%= link_to 'Sign Up', new_user_path %>
          </li>
          <li>
            <%= link_to 'Login', login_path %>
          </li>
        <% end %>
      </ul>
      <ul class='nav navbar-nav pull-left'>
      <% if params[:controller] == 'meeting' || params[:controller] == 'posts' %>
        <li>
          <%= link_to @city.name, city_path(@city) %>
        </li>
      </ul>
      <% end %>
    </div>
  </div>
</nav>

<!-- Header -->
<div class="wide">
  	<div class="col-xs-5 line"><hr></div>
    <div class="col-xs-2 logo">Knock Knock</div>
    <div class="col-xs-5 line"><hr></div>
</div>

<div class="container">
  <div class="text-center">
    <h1>Log in to book a tutor</h1>
  </div>





<!-- will move the landing page to another place later -->



<div class="containter">
	<div class="row">
		<div class="col-sm-6 col-sm-offset-3 user_signup_form">
			<%= form_for @user do |f| %>
				<h3>Sign Up</h3>
				<div class="form-group">
					<%=  f.label :name %>
					<%=  f.text_field :name, class: "form-control", autofocus: true %>
				</div>
				<div class="form-group">
					<%=  f.label :email %>
					<%=  f.email_field :email, class: "form-control", autofocus: true %>
				</div>
				<div class="form-group">
					<%=  f.label :type, "Role" %>
					<%=  f.collection_select :type, ["Ta", "Student"], :to_s, :to_s,class: "form-control", autofocus: true %>
				</div>
				<div class="form-group">
					<%=  f.label :password %>
					<%=  f.password_field :password, class: "form-control", autofocus: true %>
				</div>
				<div class="form-group">
					<%=  f.label :password_confirmation %>
					<%=  f.password_field :password_confirmation, class: "form-control", autofocus: true %>
				</div>
				<div class="form-group">
					<%= f.submit "Sign Up", class: "btn btn-primary form-control signup_button" %>
				</div>
			<% end %>
			<p class="text-center">Already have an account? <%= link_to "Click Here", login_path %> to log in.</p>
		</div>
	</div>
</div>
